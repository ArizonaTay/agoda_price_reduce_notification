[project]
name = "agoda-price-monitor"
version = "1.0.0"
description = "Monitors hotel room prices on Agoda and sends Telegram notifications when prices drop"
requires-python = ">=3.11"
dependencies = [
    "playwright",
    "python-dotenv",
    "requests",
]
